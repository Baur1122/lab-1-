компаратор определения (значение, ключ):
    len_key = len(ключ)
    Дик = {}
    итер = 0
    полный = 0

    для я в значении:
        dic [полный] = [я, ключ [итер]]
        полный = полный + 1
        итэр = итэр +1
        если (итер >= len_key):
            итер = 0
    вернуть диск

def full_encode (значение, ключ):
    dic = компаратор (значение, ключ)
    print 'Сравнить полное кодирование', dic
    Лис = []
    д = form_dict()

    для v в dic:
        go = (dic[v][0]+dic[v][1]) % len(d)
        lis.append(перейти)
    вернуть список

защита decode_val (list_in):
    list_code = []
    одолжил = len (list_in)
    д = form_dict()

    для i в диапазоне (одолжен):
        для значения в d:
            если list_in[i] == значение:
               list_code.append (d [значение])
    вернуть list_code