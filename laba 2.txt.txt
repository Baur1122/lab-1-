1. Реализовать в программе шифрование и дешифрование свойства файла по методу Цезаря с ключевым ключом.
(С++)(Куаныш)
const alf='ABCDEFGHIJKLMNOPQRSTUVWXYZ';//алфавит
вар
  Форма1: TForm1;
  с, с1: строка;
реализация
 
{$R *.dfm}
 
процедура TForm1.Button1Click(Отправитель: TObject);
var k,i,j: целое число;
    кл,с2:строка;
начинать
s:=AnsiUpperCase(Edit1.Text);
kl:=AnsiUpperCase(Edit1.Text);
Вал(Редактировать3.Текст,к,я);
если(i<>0)или нет(k в [0..24]), то
начинать
   ShowMessage('Неверно введено смещение');
   Редактировать3.Очистить;
   Редактировать3.SetFocus;
   Выход;
конец;
с1:=кл; //пишем в строке ключ
for i:=1 to k do s1:=' '+s1;//в начало 17 пробелов
s2:='';//алфавит без буквы ключевого слова
для i:= от 1 до 26 сделать
если pos(alf[i],kl)=0, то s2:=s2+alf[i];
i:=length(s1)+1;//конец строки+1
j:=length(s1);//длина строки
в то время как length(s1)<=26 do //пока не 26
начинать
  s1:=s1+s2[ij];//добавляем первые буквы алфавита
  я:=я+1;
конец;
в то время как s1[1]=' 'удалить (s1,1,1);
s1:=копия(s2,ij,длина(s2))+s1;
//шифрование
для i:=1 до длины(s) do
s[i]:=s1[pos(s[i],alf)];
Редактировать4.Текст:=с;
конец;
процедура TForm1.Button2Click(Отправитель: TObject);
переменная i: целое число;
начинать
для i:=1 до длины(s) do
s[i]:=alf[pos(s[i],s1)];
Редактировать5.Текст:=s;
конец;